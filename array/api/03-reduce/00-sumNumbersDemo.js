/**
 * CONSTANTS
 */
const partialTotals = [1, 2, 3, 4, 5];

/**
 * Given 'sumPartialTotals' array, define function 'getTotal' to return total.
 * By default add 10 to the total
 *
 * expected: 25
 */
