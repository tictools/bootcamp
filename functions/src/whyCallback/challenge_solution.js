/**
 * CHALLENGE :: STEP#3 :: CALLBACKS
 * Write fuctions to be compliant with requirements
 * You can reuse here any function defined in STEP#1 or create your own ones
 * Transform description (input | output) into function signature (use JSDocs)
 * Execute each function and log its result
 * Load file as script in 'index.html' to show the result
 */

//place here auxiliar functions which encapsulate logid required by main functions

/**
 * Write JS function "getMillisecondsForCurrentDate" to get milliseconds of now time.
 * 
 * hint: use 'new Date and getMilliseconds from Date API'
 * doc: https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date
 * doc: https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/getMilliseconds
 * 
 * Function "getMillisecondsForCurrentDate" will receive a callback as argument.
  * input: function (callback) 
  * output: number
 * 
 * Callback function will receive milliseconds as argument and execute an action once date milliseconds have been obtained:
  - if milliseconds is greater or equal than 500, then an alert will be raised with message "Date milliseconds is greater or equal than 500"
  - otherwise, an alert will be raised with message "Date milliseconds is less than 500"
  * input: number (milliseconds) 
 */

/**
 * Write JS function "transformArrayIntoString" to abtain a string from an array.
 * 
 * Function "transformArrayIntoString" will receive one array of words (use ['This', 'is', 'an', 'array', 'of', 'words']) and one callback as arguments.
  * input: Array (collection) 
  * output: string
 * 
 * Callback function will receive an string as argument and it execute an action once array has been transformed into string:
  - uppercase string and log it
 
 * "callback"
  * input: string (sentence) 
 */
